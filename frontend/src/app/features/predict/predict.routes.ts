import { Routes } from '@angular/router';
import { PredictPageComponent } from './pages/predict-page/predict-page.component';

export const PREDICT_ROUTES: Routes = [
  { path: '', component: PredictPageComponent }
];
